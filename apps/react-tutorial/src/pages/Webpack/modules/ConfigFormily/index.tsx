import React from 'react';
import SimpleForm from './components/SimpleForm';
import './styles';
import './index.less';

const ConfigFormily: React.FC = () => {
  return <SimpleForm />;
};

export default React.memo(ConfigFormily);