# 自定义云服务配置示例
# 复制此文件并重命名为 custom-cloud.env，然后根据您的云服务提供商进行配置

# 基本配置
SERVERLESS_PROVIDER=custom
CUSTOM_PROVIDER=huawei  # 自定义提供商名称，例如：huawei, baidu, jd 等
CUSTOM_REGION=cn-north-4  # 自定义区域
CUSTOM_RUNTIME=nodejs18
CUSTOM_HANDLER=src/serverless/adapter.customHandler

# 函数配置
MEMORY_SIZE=1024
TIMEOUT=30

# 静态资源配置
CUSTOM_ASSETS_PREFIX=https://your-custom-cdn.com
CUSTOM_ASSETS_DEPLOY_CMD="your-cloud-cli upload -r ./dist/client/ your-bucket-name"

# 缓存配置
CACHE_STORAGE=memory  # 或 redis
CACHE_TTL=60000
CACHE_MAX_SIZE=1000
CACHE_NAMESPACE=custom-ssr-cache

# Redis配置（如果使用Redis缓存）
REDIS_HOST=your-redis-host
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password
REDIS_DB=0

# 自定义环境变量（将传递给云函数）
CUSTOM_ENV_VAR1=value1
CUSTOM_ENV_VAR2=value2

# 自定义部署命令（如果需要）
# 如果您的云服务提供商需要特殊的部署命令，可以在这里配置
# 这些命令将在 deploy.sh 脚本中被使用
CUSTOM_DEPLOY_CMD="your-cloud-cli deploy --function-name your-function"
CUSTOM_ASSETS_DEPLOY_CMD="your-cloud-cli upload -r ./dist/client/ your-bucket-name"

# 自定义API网关配置
CUSTOM_API_GATEWAY_ID=your-api-gateway-id
CUSTOM_API_GATEWAY_STAGE=release
CUSTOM_API_GATEWAY_BASE_PATH=/api

# 自定义监控配置
CUSTOM_MONITORING_URL=https://your-monitoring-service.com
CUSTOM_LOGGING_URL=https://your-logging-service.com
